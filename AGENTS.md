# 项目规则

## Git 提交

- Commit message 必须清楚说明修改点及采用的技术方案；在信息完整、表达清楚的前提下尽量精简。
- 普通提交和分支 push 不添加 `[skip ci]`；自动流程是否运行由工作流的 tag 触发条件控制。
- 提交和 push 只核对必要的改动、分支及待推送提交；同一会话内已确认的远端和权限直接复用。除非出现新改动、目标变化或推送错误，不重复全量 diff 审阅、CI 配置检查或账号权限查询。

## 自动构建与发布

- 仅推送 `v主版本.次版本.修订号` 的版本 tag 时，触发完整的测试、Debug/Release 编译、打包和发布流程。
- 普通分支 push、PR 不触发上述流程，不启用 `workflow_dispatch` 入口。
- 日常修改、提交和 push 不主动运行测试、Lint 或静态检查；仅在用户明确要求或打 tag 发版时执行相关检查。
