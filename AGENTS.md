# 项目规则

## Git 提交

- Commit message 必须清楚说明修改点及采用的技术方案；在信息完整、表达清楚的前提下尽量精简。
- 普通提交和分支 push 不添加 `[skip ci]`；自动流程是否运行由工作流的 tag 触发条件控制。
- 用户已长期授权：在本仓库要求提交或 push 时，将本次任务范围内的提交以普通、非强制方式推送到现有 `origin`（`https://github.com/bobcatkay/yuvraw-viewer.git`）。该授权跨会话有效，无需重复询问是否允许向此仓库推送，也无需每个会话重新验证账号权限。
- 提交和 push 只核对必要的改动、分支及待推送提交；复用已确认的远端与授权，不重复全量 diff 审阅、CI 配置检查或账号权限查询。远端变更或推送报错时再按具体原因处理；此授权不包含强制推送、删除远端引用或未经请求打 tag 发版。
- 上述长期授权不能关闭平台自动审批；若审批仍拦截推送，应使用已有授权及可用证据说明情况，不绕过平台限制。

## 自动构建与发布

- 仅推送 `v主版本.次版本.修订号` 的版本 tag 时，触发完整的测试、Release 编译、打包和发布流程；流水线不构建 Debug。
- 普通分支 push、PR 不触发上述流程，不启用 `workflow_dispatch` 入口。
- 日常修改、提交和 push 不主动运行测试、Lint 或静态检查；仅在用户明确要求或打 tag 发版时执行相关检查。
