[English](CONTRIBUTING.md) | 简体中文

# 贡献指南

欢迎提交可复现的问题、文档改进和范围明确的代码修改。新功能或较大架构变动先通过 Issue 描述使用场景、输入参数、预期行为和验证办法。

## 开始开发

1. 按[构建说明](docs/BUILDING.zh-CN.md)安装 Windows C++ 工具链并构建 `YUVRaw.sln`。
2. 阅读[架构](ARCHITECTURE.zh-CN.md)，确认数据属于哪个文档、在哪个线程访问。
3. 完成必要修改及相关回归；在 PR 中说明具体问题、最终行为和实际执行的验证。

C++ 类名使用 `F` 前缀，枚举使用 `E` 前缀，函数/成员使用 PascalCase，布尔成员使用 `b` 前缀。头文件使用 `#pragma once`。有业务含义的常量应命名，关键并发、边界和兼容逻辑的注释解释原因。日志使用已有统一入口，不在逐帧路径持续输出，不写入凭证或完整敏感数据。

新增源码应加入 `.vcxproj` / `.filters` 并同步源码打包输入。新增格式枚举只追加，色彩变更同时检查 CPU 和 GLSL 实现。提交信息简洁说明改动和采用的技术方案。公开文档保持中英文同步，UI 文本统一加入共享双语资源。
## Pull Request

- 一个 PR 解决一个清晰问题；文档写最终行为，不保留过时方案。
- 说明测试结果及 `PASS` / `FAIL` / `SKIP` 的原因，不能把缺少 GPU、编解码器或真实素材写成通过。
- 依赖更新要同步版本清单、来源锁、配方/补丁、许可证、SBOM 和对应源码交付；重跑 DNG、色彩及导出回归。
- 合并前保持分支与主分支同步，通过 Debug 和 Release CI 检查，并解决审阅讨论。

提交贡献即表示你有权公开该内容，并同意将项目自有代码贡献按 `GPL-3.0-only` 分发。引入第三方代码时保留来源和原许可，先核查兼容性。安全问题按 [SECURITY.md](SECURITY.zh-CN.md) 处理。
